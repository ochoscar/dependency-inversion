[tool.poetry]
name = "dependency-inversion-python"
version = "0.1.0"
description = ""
authors = ["Oscar Andres Ochoa Villegas <ochoscar@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
pytest = "^8.4.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest-timer (>=1.0.0,<2.0.0)",
    "pytest-sugar (>=1.1.1,<2.0.0)"
]
